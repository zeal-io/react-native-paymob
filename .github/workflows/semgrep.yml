name: Semgrep
# DISABLED: will be enabled when semgrep.dev subscription expires
# To enable: replace workflow_dispatch with push trigger below
on:
  workflow_dispatch: {}
  # push:
  #   branches:
  #     - main
jobs:
  semgrep:
    uses: zeal-io/zeal-workflows/.github/workflows/semgrep.ci.yml@main
    secrets: inherit